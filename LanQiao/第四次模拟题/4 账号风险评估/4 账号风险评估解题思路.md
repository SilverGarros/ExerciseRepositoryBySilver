需要完成的函数 logistic_regression_model 如下：

```python	
def logistic_regression_model(X_train, y_train, X_test, y_test, dist):
    #TODO
    # 训练逻辑回归模型
    lr = LogisticRegression()
    lr.fit(X_train,y_train)
    # 保存训练后的模型为.pkl文件
    with open(dist,'wb')as f:
        pickle.dump(lr,f)   
    # 使用训练后的模型进行测试机预测
    y_test_pre = lr.predict(X_test)
    # 计算F1得分
    f1_sc = f1_score(y_test,y_test_pre)

    return f1_sc
```

